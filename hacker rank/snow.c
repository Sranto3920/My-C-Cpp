#include<stdio.h>
#include<string.h>
int main (){
    int shelves , request ;
    scanf("%d %d ",&shelves , & request);
    
    int j , arr[j] ; 
    for ( int i = 0 ; i < request ; i++){

        if (i < request - 1) {
            for (  j = 0 ; j < 3 ; j++ ) {            
            scanf ( " %d ", &arr[j]) ;
            }
        }

        else {
            for (  j = 0 ; j < 2 ; j++ ) {            
                scanf ( " %d ", &arr[j]) ;
                } 
        }
    }







    
}